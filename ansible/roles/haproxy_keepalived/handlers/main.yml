---
- name: "reload percona-haproxy"
  ansible.builtin.systemd:
    name: percona-haproxy
    state: reloaded

- name: "restart keepalived"
  ansible.builtin.systemd:
    name: keepalived
    state: restarted
    daemon_reload: true

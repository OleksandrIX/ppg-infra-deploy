---
- name: "reload haproxy"
  ansible.builtin.systemd:
    name: haproxy
    state: reloaded

- name: "restart keepalived"
  ansible.builtin.systemd:
    name: keepalived
    state: restarted
    daemon_reload: true

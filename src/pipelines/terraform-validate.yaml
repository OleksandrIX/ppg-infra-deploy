steps:
- task: AzureCLI@2
  displayName: 'Validate Terraform code'
  inputs:
    azureSubscription: $(azureSubscription)
    scriptType: 'bash'
    scriptLocation: 'inlineScript'
    workingDirectory: 'envs/${{ parameters.environment }}/terraform'
    inlineScript: |
      terraform validate
  env:
    TF_VAR_pgbackrest_azure_key: $(pgbackrest_azure_key)
